# 3.2 结构框架

本研究围绕工业设备异步多传感器状态预测与风险评估问题展开，论文整体结构按照“异步观测建模与规整表示、联合概率状态预测、工业风险评估与鲁棒性验证”的递进逻辑组织。全文结构安排如下。

## 摘要

## Abstract

## 第一章 绪论

### 1.1 研究背景及研究意义

### 1.2 工业设备状态预测与风险评估研究现状

#### 1.2.1 工业设备状态预测与剩余寿命研究现状

#### 1.2.2 不规则时间序列与异步缺失建模研究现状

#### 1.2.3 概率预测、不确定性量化与风险评估研究现状

### 1.3 工业异步多传感器状态预测面临的主要问题

#### 1.3.1 异步采样与缺失观测导致的信息不完整问题

#### 1.3.2 多传感器联合概率建模与相关结构表达不足

#### 1.3.3 风险评估中概率输出与校准能力不足

### 1.4 本文主要研究内容与创新点

### 1.5 论文组织结构

## 第二章 相关理论与技术基础

### 2.1 工业设备状态预测与多元时间序列建模

### 2.2 异步观测与缺失时间序列建模基础

### 2.3 异步时间序列规整表示学习方法

### 2.4 条件生成式概率预测方法

### 2.5 概率预测、区间校准与风险评估指标

### 2.6 本章小结

## 第三章 工业异步多传感器观测建模与规整表示实验

### 3.1 研究动机与问题定义

### 3.2 工业异步事件序列与统一张量协议

### 3.3 MetroPT-3、C-MAPSS 与 Tennessee Eastman Process 数据集适配

### 3.4 异步缺失机制与风险标签构造

### 3.5 异步规整表示编码器

#### 3.5.1 时间戳、观测掩码与通道身份编码

#### 3.5.2 多尺度历史观测压缩与变量级状态表示

#### 3.5.3 工况条件注入与状态表示更新

### 3.6 基于轻量预测头的表示有效性实验

#### 3.6.1 实验设置、对比方法与复现协议

对比模型包括 Linear Interpolation + TCN、Forward Fill + GRU、Masked TCN、GRU-D、ODE-RNN 或 mTAN，以及本文的异步规整表示编码器 + MLP/Linear 轻量预测头。所有方法使用相同训练/验证/测试划分、相同归一化统计量、相同人工缺失 mask 和相同随机种子。本章只验证异步表示质量，不使用 KAFNet-ProFITi、ProFITiFlowHead、Joint Flow 或 RiskHead。

评价指标包括 MAE、RMSE、不同缺失率下的性能下降幅度、训练时间、推理时间和参数量。

#### 3.6.2 数据协议有效性实验

比较统一异步事件协议、规则重采样协议和直接插值协议，分析时间戳、观测掩码、通道身份和工况变量对预测性能的影响。

#### 3.6.3 插值、填充、掩码建模与规整表示对比实验

比较线性插值、前向填充、仅掩码建模、缺失衰减建模和异步规整表示建模，验证规整表示是否能够在不制造伪观测的情况下提升状态预测效果。

#### 3.6.4 多缺失率与不同缺失机制下的鲁棒性实验

设置 10%、30%、50%、70% 随机缺失，以及通道降采样、连续离线等缺失机制，比较不同表示方法在复杂观测条件下的稳定性。

#### 3.6.5 多尺度编码与工况信息消融实验

消融项包括去除多尺度窗口、去除时间戳编码、去除观测掩码、去除通道身份编码和去除工况条件。评价指标仍以 MAE、RMSE 和缺失鲁棒性下降幅度为主。

#### 3.6.6 表示编码超参数敏感性分析

分析历史窗口长度、规整表示维度、多尺度窗口组合、时间编码维度和轻量预测头类型对预测误差与计算开销的影响。该小节只讨论表示编码阶段的超参数，不涉及联合概率分布头和风险阈值。

### 3.7 本章小结

## 第四章 基于 KAFNet-ProFITi 的联合概率状态预测方法与实验

### 4.1 研究动机与问题定义

### 4.2 KAFNet-ProFITi 总体框架

### 4.3 历史状态表示与未来查询条件构造

### 4.4 QueryConditionAdapter 查询条件适配

### 4.5 ProFITiFlowHead 联合概率分布建模

### 4.6 模型训练目标与推理流程

### 4.7 实验设计与结果分析

#### 4.7.1 实验设置、对比方法与复现协议

对比模型包括 TCN-Gaussian、PatchTST-Gaussian、GRU-D、ODE-RNN 或 mTAN、GraFITi、ProFITi、KAFNet 点预测模型、KAFNet + Gaussian Head、KAFNet + marginal flow、KAFNet-ProFITi joint，以及最终增强模型 KST ProbFlow。所有模型使用相同数据划分、相同缺失 mask、相同预测窗口和相同评价脚本。

评价指标包括 MAE、RMSE、NLL、CRPS、PICP、MPIW、训练时间、推理时间和参数量。若算力允许，主实验报告 3 个随机种子的均值和标准差；若算力受限，至少保证主模型和核心基线采用相同 seed 与完整训练轮次。

#### 4.7.2 主实验结果与概率预测性能分析

统一报告点预测误差、概率预测质量和预测区间质量，避免将 MAE/RMSE、NLL/CRPS、PICP/MPIW 分散成多个重复小节。

#### 4.7.3 消融实验与模块贡献分析

消融项包括 KAFNet-ProFITi joint 与 KAFNet + Gaussian Head、marginal flow、去除工况条件、去除查询条件适配、改变查询排序、去除联合相关结构等设置。主要评价 NLL、CRPS、PICP、MPIW 和 MAE/RMSE 的变化。

#### 4.7.4 联合概率预测超参数敏感性分析

分析 flow 层数、hidden dimension、query 排序方式、预测窗口长度、样本数 nsamples、联合建模秩或相关结构强度等超参数对 NLL、CRPS、PICP、MPIW。该小节用于说明模型稳定性和计算代价，不讨论风险报警阈值。

#### 4.7.5 预测区间与联合不确定性可视化分析

选择典型样本展示预测均值、95% 预测区间、真实值和多传感器联合波动样本，用于解释联合概率模型对不确定性和变量相关结构的表达能力。

### 4.8 本章小结

## 第五章 面向工业运维的风险评估、校准与鲁棒性实验

### 5.1 研究动机与问题定义

### 5.2 基于预测分布的风险分数构造

### 5.3 验证集阈值选择与概率校准方法

### 5.4 风险预警实验设计

### 5.5 实验结果与分析

#### 5.5.1 风险识别与校准性能分析

对比模型沿用第四章中的代表性模型，建议包括 TCN-Gaussian、PatchTST-Gaussian、ProFITi、KAFNet-ProFITi joint 和 KST ProbFlow。评价指标包括 AUROC、AUPRC、F1、ECE、报警阈值、验证集正常样本分位阈值和校准前后风险分数变化。

#### 5.5.2 风险时间线与提前预警案例分析

展示典型设备或故障窗口中的风险分数时间线、真实风险标签、报警阈值和预测区间变化。若数据集能够提供明确故障发生时间，则报告 Lead time；若故障时间不可稳定定义，则不将 Lead time 作为核心指标，只作为案例分析。

#### 5.5.3 缺失鲁棒性实验

合并不同缺失率和不同缺失机制实验，设置 10%、30%、50%、70% 随机缺失，以及通道降采样、连续离线等机制。评价指标包括 AUROC、AUPRC、F1、ECE 和风险分数稳定性。

#### 5.5.4 风险阈值与校准超参数敏感性分析

分析验证集正常样本分位阈值、风险窗口长度、风险平滑窗口、校准方法和风险标签阈值对 AUROC、AUPRC、F1、ECE、误报率和漏报率的影响。该小节只讨论风险评估阶段的超参数，不重新比较第四章的预测误差指标。

#### 5.5.5 风险评估结果综合讨论

综合讨论规则采样概率模型、不规则时间序列概率模型、KAFNet-ProFITi joint 和 KST ProbFlow 在风险评估中的适用边界，分析预测误差、概率质量、校准性能和风险识别能力之间是否一致。本节不新增同类报警阈值表，避免与 5.5.1 重复。

### 5.6 本章小结

## 第六章 总结与展望

### 6.1 全文总结

### 6.2 研究局限性分析

### 6.3 后续研究展望

## 参考文献

## 致谢
