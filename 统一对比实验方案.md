# 统一对比实验方案

## 1. 实验目标

本实验协议面向同一个研究任务：

**工业设备异步缺失多元传感器的未来状态概率预测与风险评分。**

三个数据集均使用同一批对比模型、同一类指标和同一套结果保存格式。不同数据集只允许在以下方面不同：

- 数据集原始划分方式；
- 状态标签或风险标签构造方式；
- 时间单位和窗口长度换算；
- 是否存在官方故障标签、RUL 标签或故障报告。

不直接横向引用不同论文中的 SOTA 数值。所有模型应在同一划分、同一缺失 mask、同一归一化规则下重新训练和测试。

## 2. 统一对比模型

| 类别 | 模型 | 输入处理 | 输出形式 | 对比目的 |
|---|---|---|---|---|
| 规则采样基线 | TCN-Gaussian | 插值 + mask 通道 | 均值 + 方差 | 检验普通卷积预测模型在异步缺失下的表现 |
| 规则采样强基线 | PatchTST-Gaussian 或 iTransformer-Gaussian | 插值 + mask 通道 | 均值 + 方差 | 检验规则采样 Transformer 类模型 |
| 缺失感知模型 | GRU-D | 原始 mask + time gap | 点预测 / 高斯头 | 检验显式缺失衰减机制 |
| 连续时间模型 | ODE-RNN 或 Latent ODE | 事件时间戳 | 点预测 / 高斯头 | 检验连续时间建模能力 |
| 注意力不规则模型 | mTAN | 事件时间戳 + mask | 点预测 / 高斯头 | 检验时间注意力对不规则观测的作用 |
| 图/patch 不规则模型 | tPatchGNN | 不规则窗口 patch | 点预测 | 检验 patch 图建模能力 |
| 强不规则基线 | GraFITi | 事件序列 | 点预测 / 隐表示 | 检验已有 irregular forecasting 强基线 |
| 概率预测基线 | ProFITi | 事件序列 | 联合 / 边际概率 | 检验条件 flow 概率预测能力 |
| 表示基线 | KAFNet | 异步规整表示 | 点预测 | 检验 KAFNet 规整表示能力 |
| 消融 1 | KAFNet + Gaussian Head | KAFNet 编码器 | 独立高斯分布 | 检验 flow 是否必要 |
| 消融 2 | KAFNet + ProFITi Marginal Flow | KAFNet 编码器 | 边际 flow | 检验联合建模是否必要 |
| 最终模型 | KAFNet + ProFITi Joint Flow | KAFNet 编码器 | 联合 flow | 验证最终方法 |

说明：

- 点预测模型统一增加同一个 diagonal Gaussian head，否则 NLL、CRPS、PICP 等概率指标不可比。
- 主实验表至少包含前 10 个模型。
- 消融实验表重点比较 `KAFNet`、`KAFNet + Gaussian Head`、`KAFNet + ProFITi Marginal Flow`、`KAFNet + ProFITi Joint Flow`。

## 3. 统一划分原则

| 项目 | 规则 |
|---|---|
| 划分顺序 | 先划分 train / valid / test，再生成滑动窗口 |
| 归一化 | 只使用 train 集统计量 |
| 缺失构造 | 所有模型使用同一套人工异步 mask |
| 主缺失设置 | 30% 随机缺失 + 通道级降采样 + 连续离线 |
| 鲁棒性缺失率 | 0%、10%、30%、50%、70% |
| 随机种子 | `2026`、`2027`、`2028` |
| 窗口设置 | 默认 `history=96`，`horizon=24` |
| 结果汇总 | 报告 `mean ± std` |

人工异步缺失构造应保存为文件，避免不同模型使用不同缺失样本。

### 3.1 MetroPT-3 正式风险对比口径

MetroPT-3 风险预警表中的模型比较统一采用以下约束：

```text
同一数据协议：metropt3_chrono_502030
同一 dataset split
同一 seed
同一 missing mask
同一 risk label rule
同一 q = 0.95
同一使用 Platt calibration
同一 checkpoint selection 规则
```

对应到当前实验实现：

| 项目 | 固定规则 |
|---|---|
| 数据协议 | `metropt3_chrono_502030`，chronological 50/20/30，无跨边界窗口 |
| dataset split | 所有模型读取同一 split 规则生成 train / valid / test |
| seed | 默认 `2026`；多次实验使用 `2026, 2027, 2028` 同一组 seeds |
| missing mask | 同一 dataset、seed、missing rate 复用同一 `.npz` 文件 |
| risk label rule | 使用同一 MetroPT 故障/预警窗口标签规则 |
| q | `q=0.95`，阈值来自 validation normal risk scores 的 95% 分位数 |
| Platt calibration | 每个模型只在 validation split 上拟合 Platt calibration，然后应用到 test |
| checkpoint selection | 默认使用 validation CRPS 最优 checkpoint，即 `checkpoint_seed{seed}_best.pt` |

因此，正式表格中的 F1/ECE 不是由 test label 搜索阈值得到。`AUROC/AUPRC` 直接用 test risk scores 的排序能力；`F1` 使用 Platt 后分数和 `validation_normal_q95` 阈值；`ECE` 使用 Platt 后风险概率。

当前已完成的 MetroPT-3 风险对比结果如下：

| Model | AUROC ↑ | AUPRC ↑ | F1 ↑ | ECE ↓ | Precision ↑ | Recall ↑ | TP | FP | TN | FN |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| KST ProbFlow | 0.9994 | 0.8591 | 0.8214 | 0.0043 | 0.7931 | 0.8519 | 23 | 6 | 7549 | 4 |
| TCN-Gaussian | 0.9915 | 0.1845 | 0.3137 | 0.0478 | 0.1905 | 0.8889 | 24 | 102 | 7453 | 3 |

该表对应的测试集统计为：

| Item | Value |
|---|---:|
| Validation windows | 5054 |
| Validation positives | 329 |
| Test windows | 7582 |
| Test positives | 27 |
| Test positive rate | 0.0036 |

推荐缺失机制：

| 缺失机制 | 说明 |
|---|---|
| 随机缺失 | 对观测点按比例随机删除 |
| 通道级降采样 | 不同传感器使用不同采样间隔 |
| 连续离线 | 随机选择传感器连续缺失一段时间 |
| 故障相关缺失 | 在故障前后提高缺失概率，仅用于鲁棒性实验 |

## 4. 三个数据集划分方式

### 4.1 MetroPT-3(使用metropt3_chrono_502030，即是5：2：3进行划分)

数据源：[UCI MetroPT-3 Dataset](https://archive.ics.uci.edu/dataset/791/metropt%2B3%2Bdataset)


MetroPT-3 原始数据为 1Hz 规则采样，没有原生缺失。用于本课题时，需要人工构造异步缺失。

### 4.2 C-MAPSS

数据源：[NASA PCoE C-MAPSS](https://www.nasa.gov/intelligent-systems-division/discovery-and-systems-health/pcoe/pcoe-data-set-repository/)

| 子集 | Train engines | Test engines | 工况 | 故障模式 |
|---|---:|---:|---:|---:|
| FD001 | 100 | 100 | 1 | 1 |
| FD002 | 260 | 259 | 6 | 1 |
| FD003 | 100 | 100 | 1 | 2 |
| FD004 | 249 | 248 | 6 | 2 |

| 部分 | 划分方式 |
|---|---|
| Train | 官方 `train_FD00x.txt` 中 80% engines |
| Valid | 官方 `train_FD00x.txt` 中 20% engines |
| Test | 官方 `test_FD00x.txt` + `RUL_FD00x.txt` |
| 划分单位 | 必须按 engine id 划分，不能按行随机划分 |
| 标签 | 训练 RUL 由 run-to-failure 序列生成 |
| RUL cap | 建议使用 125 或 130，并在实验配置中固定 |
| 风险标签 | 可将 `RUL <= threshold` 定义为风险状态 |

C-MAPSS 用于验证退化过程中的概率预测能力。若主任务是未来传感器联合分布，则 RUL 作为辅助风险标签。

### 4.3 Tennessee Eastman Process

数据源：[Harvard Dataverse TEP](https://dataverse.harvard.edu/dataset.xhtml?persistentId=doi:10.7910/DVN/6C3JR1)

| 部分 | 划分方式 |
|---|---|
| Train | 官方 training runs 中 80% `simulationRun` |
| Valid | 官方 training runs 中 20% `simulationRun` |
| Test | 官方 testing runs |
| 划分单位 | 必须按 `simulationRun` 划分，不能按时间点随机划分 |
| 正常样本 | fault-free runs 或 faulty testing 中故障注入前正常段 |
| 故障样本 | faulty testing 中故障注入后的样本 |
| 任务 | 过程变量预测 + 故障风险评分 |

如果采用扩展版 TEP，常见设定是 training run 较短、testing run 较长，testing run 中前段为正常过程，之后进入故障过程。具体故障注入点应以所下载版本说明为准，并写入配置文件。

## 5. 统一评价指标

| 指标族 | 指标 | 用途 |
|---|---|---|
| 点预测 | MAE、RMSE | 未来传感器数值预测 |
| 概率预测 | NLL、CRPS、Energy Score | 分布预测质量 |
| 区间预测 | PICP、MPIW | 预测区间覆盖率和区间宽度 |
| 风险/预警 | AUROC、AUPRC、F1、Precision、Recall | 正常 / 预警 / 异常区分 |
| 校准 | ECE、Reliability Curve | 风险概率是否可信 |
| 工业可用性 | Lead Time、FAR、FDR | 提前预警、误报率、检出率 |
| 效率 | 参数量、训练时间、推理时间、显存 | 工程复杂度 |

主表建议只放：

```text
MAE, RMSE, NLL, CRPS, PICP, AUROC, AUPRC, F1
```

附表补充：

```text
Energy Score, MPIW, ECE, Lead Time, FAR, FDR, 参数量, 训练时间, 推理时间, 显存
```

### 5.1 指标优劣判断口径

主任务是未来多传感器联合概率预测，风险预警是辅助任务。因此指标解释应分层进行：

| 指标 | 趋势 | 判断方式 |
|---|---|---|
| MAE | 越低越好 | 衡量预测均值的平均绝对误差，反映整体点预测偏差 |
| RMSE | 越低越好 | 对大误差窗口更敏感；若 RMSE 远高于 MAE，说明少量窗口误差较大 |
| NLL | 越低越好 | 衡量真实值在预测分布下的概率密度；必须同时确认无 NaN/Inf 和无长尾爆炸 |
| CRPS | 越低越好 | 综合均值误差和分布形状，比 NLL 对极端密度更稳健 |
| PICP | 接近目标覆盖率更好 | 95% 区间实验中理想值接近 0.95；明显低于 0.95 表示不确定性偏窄 |
| MPIW | 在 PICP 达标前提下越低越好 | 区间宽度指标，不能单独追求变小，否则会牺牲覆盖率 |
| AUROC | 越高越好 | 衡量风险分数排序能力；正负类都存在时才可解释 |
| AUPRC | 越高越好 | 适合故障正类稀少场景，通常比 AUROC 更能反映预警实用性 |
| F1 | 越高越好 | 受阈值影响大，必须说明阈值来源 |
| ECE | 越低越好 | 衡量风险概率校准误差，越低表示分数刻度越可信 |
| Lead Time | 越早越好 | 需同时约束误报率；只在有明确故障事件时间时报告 |

风险阈值默认不使用 test label 选择。当前统一入口默认采用 **validation 正常样本风险分数分位数** 定阈值，也就是按“报警预算”定阈值：

```text
risk_threshold = quantile(validation_normal_risk_scores_after_platt, 0.95)
risk_threshold_source = validation_normal_quantile
risk_calibration_method = platt
risk_calibration_quantile = 0.95
calibration_uses_test_labels = false
```

`validation_best_f1` 和 `test best_f1` 只作为诊断字段。论文或报告中不能把 test split 上搜索得到的 `best_f1` 当作正式 F1。

风险指标需要满足正负类条件。若 test split 中没有故障正类，`AUROC/AUPRC/F1` 应报告为 `null` 或 `not applicable`，只分析预测指标和风险分数趋势。

## 6. 要保存的表

| 表名 | 内容 | 行 | 列 |
|---|---|---|---|
| Table 1 Dataset Split | 三个数据集的划分统计 | 数据集 | train / valid / test 样本数、窗口数、传感器数、缺失率 |
| Table 2 Main Forecasting | 主预测结果 | 数据集 × 模型 | MAE、RMSE、NLL、CRPS |
| Table 3 Risk Prediction | 风险预警结果 | 数据集 × 模型 | AUROC、AUPRC、F1、ECE、Lead Time |
| Table 4 Missing Robustness | 缺失鲁棒性 | 数据集 × 模型 × 缺失率 | MAE、NLL、AUROC |
| Table 5 Ablation | 消融实验 | 模型变体 | MAE、RMSE、NLL、CRPS、AUROC |
| Table 6 Efficiency | 效率对比 | 模型 | 参数量、训练时间、推理时间、显存 |
| Table 7 Statistical Test | 统计检验 | 数据集 × 模型 | mean ± std、p-value |

表格文件建议保存为：

```text
tables/
  table1_dataset_split.csv
  table2_main_forecasting.csv
  table3_risk_prediction.csv
  table4_missing_robustness.csv
  table5_ablation.csv
  table6_efficiency.csv
  table7_statistical_test.csv
```

## 7. 要保存的图

| 图名 | 内容 | 文件名 |
|---|---|---|
| Fig. 1 Architecture | `KAFNetEncoder -> QueryConditionAdapter -> ProFITiFlowHead -> RiskScoring` | `fig1_architecture.png/svg` |
| Fig. 2 Split Timeline | 三个数据集的时间 / engine / run 划分示意 | `fig2_split_timeline.png/svg` |
| Fig. 3 Missing Robustness | 缺失率 vs MAE / NLL / AUROC 折线图 | `fig3_missing_robustness.png/svg` |
| Fig. 4 Prediction Interval | 真实值、预测均值、95% 预测区间 | `fig4_prediction_interval.png/svg` |
| Fig. 5 Risk Timeline | 故障前后风险分数随时间变化 | `fig5_risk_timeline.png/svg` |
| Fig. 6 Calibration | reliability curve + ECE | `fig6_calibration.png/svg` |
| Fig. 7 Ablation Bar | 消融模块对 NLL / CRPS / AUROC 的影响 | `fig7_ablation.png/svg` |
| Fig. 8 Efficiency Pareto | 推理时间 vs NLL 或 AUROC | `fig8_efficiency_pareto.png/svg` |

图文件建议同时保存 PNG 和 SVG。

## 8. 原始结果保存格式

每次实验应保存配置、划分、缺失 mask、预测样本和指标。

当前代码默认按 `output_dir/run_id/` 保存一次完整实验。`run_id` 建议包含数据集名和时间戳，例如：

```text
metropt3_chrono502030_mskaf_20260609_084658
cmapss_fd004_mskaf_20260609_160000
```

实际目录结构：

```text
{output_dir}/{run_id}/
  splits/
    {dataset}/{model}/{dataset}_split_seed{seed}.json
  masks/
    {dataset}/{dataset}_missing_{rate}_seed{seed}.npz
  metrics/
    {dataset}/{model}/metrics_seed{seed}.json
  predictions/
    {dataset}/{model}/mean_seed{seed}.npy
    {dataset}/{model}/samples_seed{seed}.npy
    {dataset}/{model}/risk_seed{seed}.npy
  tables/
    table1_dataset_split.csv
    table2_main_forecasting.csv
    table3_risk_prediction.csv
    table4_missing_robustness.csv
    table5_ablation.csv
    table6_efficiency.csv
    table7_statistical_test.csv
  figures/
    fig1_architecture.png
    fig1_architecture.svg
    fig2_split_timeline.png
    fig2_split_timeline.svg
    fig3_missing_robustness.png
    fig3_missing_robustness.svg
    fig4_prediction_interval.png
    fig4_prediction_interval.svg
    fig5_risk_timeline.png
    fig5_risk_timeline.svg
    fig6_calibration.png
    fig6_calibration.svg
    fig7_ablation.png
    fig7_ablation.svg
    fig8_efficiency_pareto.png
    fig8_efficiency_pareto.svg
  configs/
    {dataset}/{model}/config_seed{seed}.yaml
  calibration/
    {dataset}/{model}/calibration_seed{seed}.json
  training_history/
    {dataset}/{model}/history_seed{seed}.json
  checkpoints/
    {dataset}/{model}/checkpoint_seed{seed}.pt
    {dataset}/{model}/checkpoint_seed{seed}_best.pt
```

其中 `checkpoint_seed{seed}.pt` 是最后一个 epoch 的模型，`checkpoint_seed{seed}_best.pt` 是按 validation CRPS 选择的 best-valid checkpoint。默认最终测试使用 best-valid checkpoint。

每个 `metrics_seed{seed}.json` 至少包含：

```json
{
  "dataset": "",
  "model": "",
  "seed": 0,
  "missing_rate": 0.3,
  "history": 96,
  "horizon": 24,
  "mae": null,
  "rmse": null,
  "nll": null,
  "crps": null,
  "picp": null,
  "mpiw": null,
  "auroc": null,
  "auprc": null,
  "f1": null,
  "ece": null,
  "lead_time": null,
  "train_time_sec": null,
  "infer_time_ms_per_batch": null,
  "num_params": null,
  "gpu_memory_mb": null
}
```

## 9. 实验矩阵

主实验矩阵：

| 数据集 | 模型数量 | 随机种子 | 缺失设置 | 输出 |
|---|---:|---:|---|---|
| MetroPT-3 | 12 | 3 | 主缺失设置 30% | 预测 + 风险 |
| C-MAPSS | 12 | 3 | 主缺失设置 30% | RUL / HI / 传感器预测 + 风险 |
| TEP | 12 | 3 | 主缺失设置 30% | 过程变量预测 + 故障风险 |

鲁棒性实验矩阵：

| 数据集 | 模型 | 缺失率 | 指标 |
|---|---|---|---|
| MetroPT-3 / C-MAPSS / TEP | 选择 5 个核心模型 | 0%、10%、30%、50%、70% | MAE、NLL、AUROC |

核心模型建议为：

```text
TCN-Gaussian
GRU-D
mTAN
ProFITi
KAFNet + ProFITi Joint Flow
```

## 10. 当前代码训练入口

当前实现位于：

```text
/home/work/new_work/code/run_experiment.py
```

已启用模型：

```text
kaf_profiti_joint
kst_probflow
```

其中 `kst_probflow` 是当前主模型，包含 MultiScaleKAFEncoder、动态 sensor graph、low-rank probability head、quantile head 和 risk head。其他对比模型当前只在 registry 中注册为 `not_implemented`，不能生成正式对比结果。

### 10.1 MetroPT-3 风险可评估协议训练

`metropt3_chrono_502030` 使用 chronological 50/20/30 划分，test split 中保留故障正类，可用于风险指标评估。

正式风险对比默认使用该协议，并固定同一 split、seed、missing mask、risk label rule、`q=0.95`、Platt calibration 和 best-valid checkpoint selection。若某个模型不能输出风险概率，需要先定义可复现的连续 risk score，再按相同 validation-only 校准流程计算 F1/ECE。

```bash
cd /home/work/new_work

TMPDIR=/tmp CUDA_VISIBLE_DEVICES=0 /root/anaconda3/bin/conda run -n torch23 python -u /home/work/new_work/code/run_experiment.py \
  --dataset metropt3_chrono_502030 \
  --model kst_probflow \
  --seed 2026 \
  --missing-rate 0.3 \
  --missing-mode mixed \
  --history-len 168 \
  --pred-len 24 \
  --stride 60 \
  --data-root /home/work/new_work/dataset \
  --output-dir /home/work/new_work/result \
  --run-id metropt3_chrono502030_mskaf_$(date +%Y%m%d_%H%M%S) \
  --epochs 50 \
  --batch-size 128 \
  --max-train-batches 0 \
  --max-eval-batches 0 \
  --nsamples 100 \
  --device cuda \
  --hidden-dim 64 \
  --te-dim 10 \
  --kernel-count 4 \
  --n-layers 2 \
  --n-heads 2 \
  --preconv-dim 16 \
  --patch-lens 12,24,48 \
  --graph-layers 1 \
  --copula-rank 32 \
  --lambda-point 0.5 \
  --lambda-quantile 0.2 \
  --lambda-risk 0.05 \
  --lr 2e-4 \
  --weight-decay 1e-4 \
  --sample-clip 30.0 \
  --attention-diag-floor 0.05
```

### 10.2 C-MAPSS FD004 训练

FD004 使用官方 test 文件作为 test，官方 train 文件按 engine id 划分 train/valid。

```bash
cd /home/work/new_work

TMPDIR=/tmp CUDA_VISIBLE_DEVICES=0 /root/anaconda3/bin/conda run -n torch23 python -u /home/work/new_work/code/run_experiment.py \
  --dataset cmapss_fd004 \
  --model kst_probflow \
  --seed 2026 \
  --missing-rate 0.3 \
  --missing-mode mixed \
  --history-len 50 \
  --pred-len 10 \
  --stride 1 \
  --data-root /home/work/new_work/dataset \
  --output-dir /home/work/new_work/result \
  --run-id cmapss_fd004_mskaf_$(date +%Y%m%d_%H%M%S) \
  --epochs 80 \
  --batch-size 128 \
  --max-train-batches 0 \
  --max-eval-batches 0 \
  --nsamples 100 \
  --device cuda \
  --hidden-dim 64 \
  --te-dim 10 \
  --kernel-count 4 \
  --n-layers 2 \
  --n-heads 2 \
  --preconv-dim 16 \
  --patch-lens 5,10,20 \
  --graph-layers 1 \
  --copula-rank 32 \
  --lambda-point 0.5 \
  --lambda-quantile 0.2 \
  --lambda-risk 0.05 \
  --lr 2e-4 \
  --weight-decay 1e-4 \
  --risk-threshold 30 \
  --sample-clip 30.0 \
  --attention-diag-floor 0.05
```

### 10.3 只用 checkpoint 重新评估

不重训时设置 `--epochs 0` 并传入 checkpoint：

```bash
TMPDIR=/tmp CUDA_VISIBLE_DEVICES=0 /root/anaconda3/bin/conda run -n torch23 python -u /home/work/new_work/code/run_experiment.py \
  --dataset metropt3_chrono_502030 \
  --model kst_probflow \
  --seed 2026 \
  --missing-rate 0.3 \
  --missing-mode mixed \
  --history-len 168 \
  --pred-len 24 \
  --stride 60 \
  --data-root /home/work/new_work/dataset \
  --output-dir /home/work/new_work/result \
  --run-id metropt3_chrono502030_mskaf_reeval_$(date +%Y%m%d_%H%M%S) \
  --epochs 0 \
  --batch-size 128 \
  --max-eval-batches 0 \
  --nsamples 100 \
  --device cuda \
  --checkpoint /path/to/checkpoint_seed2026_best.pt
```

### 10.4 只重评估风险阈值与校准

当只想比较阈值策略，不改变模型预测结果时，使用：

```bash
TMPDIR=/tmp /root/anaconda3/bin/conda run -n torch23 python -u /home/work/new_work/code/evaluate_risk_calibration.py \
  --run-dir /home/work/new_work/result/metropt3_chrono502030_mskaf_20260609_084658 \
  --data-root /home/work/new_work/dataset \
  --device cuda \
  --batch-size 128
```

该脚本输出 `risk_calibration_e1_seed{seed}.json`，用于比较：

```text
raw_validation_best_f1
raw_validation_normal_q90
raw_validation_normal_q95
platt_validation_best_f1
platt_validation_normal_q95
```

## 11. 方法贡献与实验对应关系

| 贡献 | 方法模块 | 对应实验 | 允许声称 |
|---|---|---|---|
| 工况感知规整表示 | KAFNetEncoder + context | 消融实验、缺失鲁棒性实验 | 在异步缺失条件下改善表示质量 |
| 联合概率预测 | ProFITi Joint Flow | 主预测实验、概率指标 | 改善 NLL、CRPS、PICP 等概率预测指标 |
| 风险预警机制 | RiskScoringHead | 风险预测实验、校准实验 | 将分布预测转化为风险分数 |
| 鲁棒性 | 固定缺失模拟协议 | 缺失率扫描实验 | 在不同缺失率下保持稳定性能 |

## 12. 数据源

- MetroPT-3：[https://archive.ics.uci.edu/dataset/791/metropt%2B3%2Bdataset](https://archive.ics.uci.edu/dataset/791/metropt%2B3%2Bdataset)
- C-MAPSS：[https://www.nasa.gov/intelligent-systems-division/discovery-and-systems-health/pcoe/pcoe-data-set-repository/](https://www.nasa.gov/intelligent-systems-division/discovery-and-systems-health/pcoe/pcoe-data-set-repository/)
- Tennessee Eastman Process：[https://dataverse.harvard.edu/dataset.xhtml?persistentId=doi:10.7910/DVN/6C3JR1](https://dataverse.harvard.edu/dataset.xhtml?persistentId=doi:10.7910/DVN/6C3JR1)
