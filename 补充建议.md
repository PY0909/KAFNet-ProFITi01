# KAFNet-ProFITi / KST ProbFlow 开题审核补充建议

## 1. 总体判断

当前方案已经具备开题的工程基础：主模型、数据适配、训练评估、风险校准、对比 baseline 框架都已经有代码支撑。

当前主要问题不是“能不能做”，而是开题材料中还需要补齐以下内容：

```text
1. 研究边界
2. 方法一致性
3. 实验公平性
4. 可复现性说明
```

否则答辩专家容易质疑：

```text
该工作到底是 KAFNet-ProFITi 缝合，
还是 KST ProbFlow 新模型，
还是多个模块叠加后的工程系统。
```

## 2. 必须补充的内容

### 2.1 统一模型命名和研究对象

文档标题仍是“KAFNet-ProFITi 缝合方案”，但当前主模型代码是：

```text
new_work/code/kaf_profiti/models/kst_probflow.py
new_work/code/kaf_profiti/experiments/registry.py
```

当前结构已经不是早期的：

```text
KAFNetEncoder -> ProFITi full joint flow
```

而是：

```text
MultiScaleKAFEncoder
-> DynamicSensorGraphBlock
-> QueryConditionAdapter
-> LowRankCopulaFlowHead
-> QuantileHead
-> RiskHead
```

建议在开题材料中明确：

```text
KAFNet-ProFITi 是初始技术路线；
KST ProbFlow 是当前实现版主模型。
```

否则“缝合感”和“当前代码实现”之间会不一致。

### 2.2 补充当前主模型与早期方案的差异表

文档前半部分仍大量描述 `ProFITi NormalizingFlow` 和 `RiskScoringHead from samples`，但当前主模型已经使用 `LowRankCopulaFlowHead + QuantileHead + RiskHead`。

建议增加如下表格：

| 模块 | 初始方案 | 当前代码实现 | 修改原因 |
|---|---|---|---|
| 编码器 | KAFNetEncoder | MultiScaleKAFEncoder | 增强长历史与多尺度局部趋势 |
| 变量关系 | FreqBlock | FreqBlock + DynamicSensorGraphBlock | 增强跨传感器依赖 |
| 概率头 | full triangular flow | Student-t marginal + low-rank copula | 降低高维 flow 不稳定 |
| 区间输出 | samples quantile | QuantileHead + samples | 改善覆盖率 |
| 风险输出 | 样本派生 | RiskHead + 校准 | 改善 F1/ECE |

### 2.3 补充风险校准代码口径不一致问题

这是目前最需要说明或修正的地方。

主工程：

```text
new_work/code/run_experiment.py
```

其中 `_fit_risk_calibration()` 默认是 validation normal quantile，但没有真正做 Platt calibration。

Platt calibration 主要在：

```text
new_work/code/evaluate_risk_calibration.py
new_work/compare_code/probabilistic_baselines/train_baseline.py
```

因此如果文档写：

```text
统一使用 Platt calibration + validation_normal_q95
```

需要说明：

```text
主训练 metrics 默认可能是 raw validation_normal_q95；
正式风险表使用 E1 / calibration 脚本输出的 platt_validation_normal_q95。
```

或者后续直接把 `run_experiment.py` 默认评估也改成 Platt 后口径。

否则专家会问：

```text
表里的 ECE/F1 到底来自哪个脚本？
```

### 2.4 补充数据集划分的合法性说明

文档已提到 `metropt3_chrono_502030` 是补充协议，但还需要更明确写清楚：

```text
它不是 UCI 官方协议；
选择它的原因是 test 中保留故障正类；
用于风险指标主评估；
官方 first-month 协议作为 stress test；
60/20/20 只适合预测指标，不适合风险指标。
```

尤其 MetroPT-3 只有少数故障事件，当前 test 风险正类来自 2020-07-15 故障窗口。需要在数据集章节补充故障事件分布表，否则 F1/AUPRC 很容易被质疑样本量不足。

### 2.5 补充对比模型状态说明

当前主 registry 里很多模型仍是 `not_implemented`：

```text
new_work/code/kaf_profiti/experiments/registry.py
```

但 `new_work/compare_code/probabilistic_baselines` 已经新增：

```text
patchtst_gaussian
ode_rnn
tpatchgnn
profiti
kafnet
```

需要在文档里说明：

```text
主工程 run_experiment.py 只启用 kst_probflow / kaf_profiti_joint；
compare_code/probabilistic_baselines 提供独立对比模型入口；
两者结果通过相同 split/mask/risk protocol 比较。
```

否则专家会看到 registry 中 `not_implemented`，认为对比实验没有落实。

## 3. 建议补充的内容

### 3.1 补充理论动机：为什么不是简单堆模块

目前方法模块很多，容易被质疑冗余。需要补一段“模块必要性”：

```text
MultiScaleKAFEncoder：解决长历史压缩和局部异常遗漏；
DynamicSensorGraphBlock：解决传感器联动关系；
LowRankCopulaFlowHead：解决 full joint flow 高维不稳定；
QuantileHead：直接优化区间覆盖；
RiskHead：避免风险完全依赖采样阈值。
```

并且每个模块都要对应一个消融实验。

### 3.2 补充消融实验矩阵

建议至少补这些消融：

| 消融 | 目的 |
|---|---|
| w/o MultiScale local patches | 验证多尺度历史是否降低 RMSE |
| w/o DynamicSensorGraphBlock | 验证传感器图关系是否提升 AUPRC |
| Gaussian head 替代 LowRankCopulaFlowHead | 验证概率头必要性 |
| w/o QuantileHead | 验证 PICP/ECE 改善是否来自分位数头 |
| w/o RiskHead | 验证风险指标是否依赖辅助监督 |
| raw q95 vs Platt q95 | 验证校准贡献 |

### 3.3 补充统计可信度

当前单 seed 结果很好，但开题材料需要承诺：

```text
seeds = 2026, 2027, 2028
报告 mean ± std
对关键指标做 bootstrap confidence interval
```

尤其 MetroPT-3 test positive 很少，F1/AUPRC 方差会很大。

建议补充：

```text
AUPRC/F1/ECE 使用 bootstrap 95% CI；
test best_f1 只作为诊断，不进入主表。
```

### 3.4 补充 NLL 解释

KST ProbFlow 的 NLL 可以为负，需要在指标解释中写明：

```text
NLL 是连续变量平均负 log-density，不是分类交叉熵，因此可能为负。
```

否则专家看到：

```text
NLL = -0.1566
```

可能误判为计算错误。

### 3.5 补充开题风险边界

建议明确三条边界：

```text
1. 主任务是未来传感器概率预测，不是纯故障分类。
2. RUL/故障标签只作为辅助风险评估，不是主监督目标。
3. 异步性来自可复现模拟，不是所有公开数据的原生属性。
```

## 4. 建议加入文档的关键表格

### 4.1 当前实现与初始方案差异

| 维度 | 初始 KAFNet-ProFITi 方案 | 当前 KST ProbFlow 实现 |
|---|---|---|
| 编码器 | KAFNetEncoder | MultiScaleKAFEncoder |
| 多尺度历史 | 独立 TemporalPatchEncoder | 合入 MultiScaleKAFEncoder |
| 传感器关系 | FreqBlock | FreqBlock + DynamicSensorGraphBlock |
| 概率头 | ProFITi full triangular flow | LowRankCopulaFlowHead |
| 区间预测 | samples quantile | QuantileHead + samples |
| 风险分数 | 样本派生 | RiskHead + validation-only calibration |
| 默认主模型名 | kaf_profiti_joint | kst_probflow |

### 4.2 MetroPT-3 数据划分说明

| 协议 | 用途 | 是否官方 | 是否适合风险指标 |
|---|---|---|---|
| `metropt3` | first-month train / remaining-months test | 接近 UCI 推荐扩展思路 | 适合 stress test |
| `metropt3_chrono_602020` | chronological 60/20/20 | 非官方 | test 无正类时不适合 |
| `metropt3_chrono_502030` | chronological 50/20/30 | 非官方补充协议 | 适合作为风险主评估 |

### 4.3 对比模型实现状态

| 模型 | 入口 | 当前状态 |
|---|---|---|
| KST ProbFlow | `new_work/code/run_experiment.py` | 主模型 |
| KAF-ProFITi Joint | `new_work/code/run_experiment.py` | 早期模型 |
| TCN-Gaussian | `new_work/compare_code/TCN-Gaussian` | 已适配 |
| PatchTST-Gaussian | `new_work/compare_code/probabilistic_baselines` | 已适配 |
| ODE-RNN | `new_work/compare_code/probabilistic_baselines` | 已适配 |
| tPatchGNN | `new_work/compare_code/probabilistic_baselines` | 已适配 |
| ProFITi | `new_work/compare_code/probabilistic_baselines` | 已适配 |
| KAFNet | `new_work/compare_code/probabilistic_baselines` | 已适配 |

## 5. 建议修改后的开题表述

可以将研究题目从：

```text
KAFNet-ProFITi 工业异步传感器状态预测缝合方案
```

调整为：

```text
面向工业异步多传感器状态预测的多尺度规整表示与概率风险建模方法研究
```

或：

```text
KST ProbFlow：面向工业异步传感器的多尺度规整表示与概率状态预测方法
```

这样可以减少“简单缝合”的印象，并且更贴合当前代码实现。

## 6. 结论

当前方案可以进入开题，但材料还需要补齐：

```text
1. 当前实现版模型说明
2. 统一风险校准口径
3. 对比模型入口
4. 数据划分合法性
5. 消融实验设计
6. 统计可信度说明
```

最关键的是把文档从“缝合方案”更新为“当前 KST ProbFlow 实现方案”，并明确：

```text
哪些结果来自训练默认 metrics；
哪些结果来自 E1 / Platt 校准复评；
哪些模型来自主工程；
哪些模型来自 compare_code 独立对比入口。
```
