name: kst_probflow
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pytorch=2.5.1
  - numpy=2.0.2
  - pandas=2.2.3
  - scikit-learn=1.6.1
  - pytest=8.4.2
  - pip
  - pip:
    - -e .  # 可选：如果需要 pip install -e
